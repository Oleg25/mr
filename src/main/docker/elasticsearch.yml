version: '2'
services:
    jogging-elasticsearch:
        image: elasticsearch:2.4.1
        # volumes:
        #     - ~/volumes/citek/jogging/elasticsearch/:/usr/share/elasticsearch/data/
        ports:
            - 9200:9200
            - 9300:9300
